This directory contains an example of a `pyproject.toml` project, used with `pydeploy`, that consists of a single module and a CLI interface. When deployed it can be invoked with the command `hello_world.exe`.